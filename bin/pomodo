#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path(File.join("..", "..", "lib"), __FILE__)
require "pomodo"

case ARGV[0]
when "init"
  exit Pomodo::Init.new(ARGV[1]).run
when "start"
  exit Pomodo::Start.new(ARGV[1] || 25, ARGV[2] || 5).run
else
  puts Pomodo::Usage.new
end
